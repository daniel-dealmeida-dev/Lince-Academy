void main(){
  final url = "https://linceacademy.com.br";
  assert(url.startsWith("https://"), "A URL deve começar com 'https://'"); 
}

// O primeiro parametro serve para a condição que queremos validar. Se for falsa, o programa para e mostra a mensagem do segundo parametro. Se for verdadeira, o programa continua normalmente.